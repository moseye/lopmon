# Lopmon Discord Bot Configuration
#
# Set your bot token and optional guild ID here.
# Values here can be overridden by environment variables (LOPMON_*)
# which in turn can be overridden by CLI flags.
#
# token = "your-discord-bot-token-here"
# guild_id = 123456789  # Optional: speeds up slash command sync during development

log_level = "INFO"
